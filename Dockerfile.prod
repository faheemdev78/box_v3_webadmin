# -------- deps --------
FROM node:20-alpine AS deps
WORKDIR /app

# Needed by some npm packages on alpine
RUN apk add --no-cache libc6-compat

COPY package.json package-lock.json* ./
RUN npm ci


# -------- builder --------
FROM node:20-alpine AS builder
ARG NEXT_PUBLIC_GRAPHQL_URI
ARG NEXT_PUBLIC_ADMIN_API_URI
ARG NEXT_PUBLIC_CDN_API_URI
ARG NEXT_PUBLIC_CDN_URL
ARG NEXT_PUBLIC_GOOGLEMAP_API_KEY
ARG NEXT_PUBLIC_REDUX_SECRET

WORKDIR /app
RUN apk add --no-cache libc6-compat

COPY --from=deps /app/node_modules ./node_modules
COPY . .

# Build Next.js
ENV NEXT_PUBLIC_GRAPHQL_URI=${NEXT_PUBLIC_GRAPHQL_URI}
ENV NEXT_PUBLIC_ADMIN_API_URI=${NEXT_PUBLIC_ADMIN_API_URI}
ENV NEXT_PUBLIC_CDN_API_URI=${NEXT_PUBLIC_CDN_API_URI}
ENV NEXT_PUBLIC_CDN_URL=${NEXT_PUBLIC_CDN_URL}
ENV NEXT_PUBLIC_GOOGLEMAP_API_KEY=${NEXT_PUBLIC_GOOGLEMAP_API_KEY}
ENV NEXT_PUBLIC_REDUX_SECRET=${NEXT_PUBLIC_REDUX_SECRET}
RUN npm run build


# -------- runner (production) --------
FROM node:20-alpine AS runner
WORKDIR /app
ENV NODE_ENV=production

RUN apk add --no-cache libc6-compat

# If you use Next.js "standalone" output, this is best.
# It will work if your next.config.js has:
#   module.exports = { output: "standalone" }
#
# We'll copy both standalone and static assets safely.
COPY --from=builder /app/public ./public
COPY --from=builder /app/.next/static ./.next/static
COPY --from=builder /app/.next/standalone ./

# If you are NOT using output:"standalone", use this instead:
# COPY --from=builder /app/package.json ./package.json
# COPY --from=builder /app/next.config.* ./  # optional
# COPY --from=builder /app/.next ./.next
# COPY --from=builder /app/node_modules ./node_modules
# COPY --from=builder /app/public ./public

EXPOSE 3000
CMD ["node", "server.js"]