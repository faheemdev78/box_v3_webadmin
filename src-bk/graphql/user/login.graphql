mutation Login($email: String!, $password: String!) {
    login(email: $email, password: $password) {
        token
        user{
            _id
            email
            name
            avatarUrl
            acc_type
            permissions
            custom_permissions
            status
        }
        isNewUser
        error { message }
    }
  }
  