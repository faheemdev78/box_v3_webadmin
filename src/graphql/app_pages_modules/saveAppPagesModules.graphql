#import "../app_pages/_fragment/app_pages_modules.graphql"

mutation saveAppPagesModules($input: [Save_AppPagesModules!]!) {
    saveAppPagesModules(input: $input) {
        ... AppPagesModulesInfo
    }
}
