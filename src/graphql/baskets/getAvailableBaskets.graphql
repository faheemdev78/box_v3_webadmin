query GetAvailableBaskets($_id_store: ID!, $limit: Int) {
  getAvailableBaskets(_id_store: $_id_store, limit: $limit) {
    baskets {
      _id
      title
      barcode
      color
      status
    }
    total_available
    error {
      message
    }
  }
}