#import "../error.graphql"

query getTotalOrdersForSlot($start_time: String!) {
    getTotalOrdersForSlot(start_time: $start_time) {
        cnt
        start_time
        error {...ErrorInfo}
    }
}
