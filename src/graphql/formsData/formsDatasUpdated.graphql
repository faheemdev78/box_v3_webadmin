#import "../_fragment/FormsData.graphql"

subscription formsDatasUpdated($filter:String){
    formsDatasUpdated(filter: $filter){
      mutation
      node {
          ... FormsDataInfo
      }
    }
}