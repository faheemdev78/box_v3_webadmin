#import "../imageSchema.graphql"

mutation uploadProductImg($_id_product: ID!, $file: ImageSchema_Input!) {
    uploadProductImg(_id_product: $_id_product, file: $file) {
        ... ImageSchemaInfo
    }
}
