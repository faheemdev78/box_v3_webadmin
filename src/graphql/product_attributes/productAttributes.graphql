#import "../_fragment/product/ProductAttributes.graphql"

query productAttributes($filter: String, $others:String) {
    productAttributes(filter: $filter, others:$others) {
        ... ProductAttributesInfo
    }
}
