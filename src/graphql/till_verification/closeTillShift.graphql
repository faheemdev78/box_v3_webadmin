#import "../_fragment/error.graphql"
#import "../_fragment/success.graphql"

mutation CloseTillShift($notes: String) {
  closeTillShift(notes: $notes) {
    success {
      ...SuccessInfo
    }
    error {
      ...ErrorInfo
    }
  }
}