#import "../_fragment/error.graphql"
#import "../_fragment/success.graphql"

mutation completeTillVerificationSession($input: CompleteTillVerificationSessionInput!) {
    completeTillVerificationSession(input: $input) {
        success {
            ...SuccessInfo
        }
        error {
            ...ErrorInfo
        }
    }
}